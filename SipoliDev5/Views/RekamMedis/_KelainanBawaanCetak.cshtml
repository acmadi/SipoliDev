@model IEnumerable<SipoliDev5.Models.ViewModels.RekamMedisKelainanBawaan>

<div class="row">
    <div class="col-lg-12" style="margin-left:60px;margin-right:20px;">
        <strong> <h4> <span class="glyphicon glyphicon-info-sign"></span> Kelainan Bawaan</h4></strong>
        <table class="table">
            <tr>
                <th style="text-align: center;">
                    @Html.DisplayNameFor(model => model.No)
                </th>
                <th style="text-align: center;">
                    @Html.DisplayNameFor(model => model.NamaKelainanBawaan)
                </th>
            </tr>

            @if (Model.Count() >= 1)
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td style="text-align: center;">
                            @Html.DisplayFor(modelItem => item.No)
                        </td>
                        <td style="text-align: center;">
                            @Html.DisplayFor(modelItem => item.NamaKelainanBawaan)
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="2"><center> <i>Tidak ada data</i> </center></td>
                </tr>
            }

        </table>
    </div>
</div>
